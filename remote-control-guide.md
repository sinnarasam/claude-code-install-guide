# Claude Code Remote Control 단독 딥다이브 📱🎛️

> 책상 앞을 떠나도 작업이 끊기지 않게 — **내 컴퓨터의 Claude Code 를 폰·태블릿·브라우저에서 그대로 이어쓰는 법**.
> "퇴근하면서도 빌드 결과를 확인하고 승인까지" 하고 싶다면 이 가이드 하나면 충분해요.

---

## 🎯 학습 목표

이 실습을 마치면 다음을 할 수 있어요:

- ✅ Remote Control 이 **무엇이고 무엇이 아닌지** 명확히 구분
- ✅ 내 플랜·버전·로그인 상태로 **시작 가능 여부 진단**
- ✅ 진입 명령 3가지(`claude remote-control` / `--remote-control` / `/remote-control`) 의 **차이와 선택 기준** 파악
- ✅ 웹·iOS·Android·VS Code 의 **표면별 능력 차이** 활용
- ✅ 보안 모델·네트워크 한계를 이해하고 **안심하고 사용**

---

## 📑 목차

- [시작하기 전에](#-시작하기-전에)
- [1. Remote Control이 뭐예요?](#1️⃣-remote-control이-뭐예요)
- [2. 시작 전 체크리스트](#2️⃣-시작-전-체크리스트)
- [3. 5분 만에 켜보기](#3️⃣-5분-만에-켜보기)
- [4. 어디서 어떻게 제어할까](#4️⃣-어디서-어떻게-제어할까)
- [5. 실전 시나리오: 카페에서 빌드 확인하기](#5️⃣-실전-시나리오-카페에서-빌드-확인하기)
- [6. 보안과 한계](#6️⃣-보안과-한계)
- [🪞 5분 회고](#-5분-회고)
- [🆘 자주 막히는 지점](#-자주-막히는-지점)
- [🎯 오늘 배운 핵심 정리](#-오늘-배운-핵심-정리)
- [📚 공식 문서](#-공식-문서)
- [🎓 다음 단계](#-다음-단계)
- [🌱 마무리: Fail Forward](#-마무리-fail-forward)

---

## 📋 시작하기 전에

- ✅ Claude Code **v2.1.51 이상** 설치 → [설치 가이드](./1-1.%20claude-code-install-guide.md)
- ✅ **claude.ai 로그인** 완료 (`/login` → claude.ai 선택) → [API 키 설정 가이드](./1-2.%20api-key-setup.md)
- ✅ 구독 플랜이 **Pro / Max / Team / Enterprise** 중 하나
- ✅ 모바일 사용 예정이면 **Claude 앱** 설치 (iOS / Android)
- ✅ 마음의 준비: **"한 번 켜두면 어디서든 이어서 일해요"** 🌱

> ⏱ **소요 시간**: 약 20~30분
> 🎯 **권장 모드**: 아무 모드 OK (설정 위주)

### 작업 폴더 준비

평소 자주 쓰는 프로젝트 폴더 하나에서 시작하면 충분해요.

```bash
cd ~/projects/my-project
claude
```

---

## 1️⃣ Remote Control이 뭐예요?

### 🤔 한마디로

내 컴퓨터에서 돌아가는 Claude Code 세션에 **다른 기기를 창처럼 붙여서**, 거기서도 메시지를 보내고 도구를 승인하고 결과를 확인하는 기능이에요.

> ⚠️ Anthropic 공식 표기: **Research preview** — 정식 GA 가 아니므로 동작·한계가 변할 수 있어요.

### 🧭 비유로 이해하기

| 익숙한 것 | Remote Control |
|----------|---------------|
| 🖨️ 사무실 프린터 + 모바일 인쇄 앱 | 인쇄 작업은 사무실 프린터에서, 명령은 폰에서 |
| 📺 거실 TV + 리모컨 | 화면(=계산)은 한 곳, 조작은 어디서든 |
| 📞 워키토키 (한 방향만 발신) | 내 컴퓨터는 **밖으로만** 신호를 쏨 (보안 핵심) |

### 🆚 비슷해 보이지만 다른 것들

| 기능 | 코드 실행 위치 | 내 파일 접근 | 비고 |
|------|---------------|-------------|------|
| **Remote Control** | 🖥️ 내 컴퓨터 | ✅ 직접 | 로컬 세션의 원격 창 |
| 🌩️ Codespaces / 클라우드 IDE | ☁️ 클라우드 VM | ❌ 동기화 필요 | 환경 자체가 클라우드 |
| 🔐 SSH | 🖥️ 원격 머신 | 그 머신의 파일 | 인바운드 포트 필요 |
| 🤖 클라우드 에이전트 | ☁️ 서비스 측 | ❌ 업로드해야 | 머신과 분리됨 |

> 💡 **핵심 차이**: Remote Control 은 **내 컴퓨터를 그대로 두고**, "조작 표면"만 다른 기기로 옮기는 거예요. 코드 실행도, MCP 서버도, 파일도 전부 내 컴퓨터에 그대로 있어요.

---

## 2️⃣ 시작 전 체크리스트

다섯 가지만 확인하면 돼요.

### ✅ 1. 버전 확인

```bash
claude --version
```

`v2.1.51` 이상이어야 해요. (VS Code 확장에서 쓰려면 **v2.1.79 이상** 권장)

```bash
# 부족하면 업데이트
claude update
```

### ✅ 2. 로그인 방식 확인

```
/login
```

→ **claude.ai** 옵션을 선택해야 해요.

> ⚠️ **API 키로는 동작하지 않아요.** 환경변수 `ANTHROPIC_API_KEY` 만 설정된 상태라면 Remote Control 메뉴 자체가 안 떠요.

### ✅ 3. 플랜 확인

| 플랜 | 사용 가능? |
|------|-----------|
| 🆓 Free | ❌ |
| 💼 Pro | ✅ |
| 🚀 Max | ✅ |
| 👥 Team | ✅ (관리자 토글 필요) |
| 🏢 Enterprise | ✅ (관리자 토글 필요) |

### ✅ 4. (Team / Enterprise 만) 관리자 토글

Team / Enterprise 플랜은 **기본값이 꺼짐**. 관리자가 한 번 켜줘야 해요.

- 위치: `claude.ai/admin-settings/claude-code`
- 항목: **"Enable Remote Control"** 토글을 ON

> 🆘 본인이 관리자가 아니면, 관리자에게 위 경로를 공유하고 켜달라고 요청하세요.

### ✅ 5. 워크스페이스 신뢰

Remote Control 을 켜기 전에 그 폴더에서 `claude` 를 한 번이라도 정상적으로 실행해, **신뢰(trust) 다이얼로그를 수락**해 둬야 해요. 안 그러면 원격에서 도구 실행이 막혀요.

---

## 3️⃣ 5분 만에 켜보기

진입하는 방법은 세 가지예요. 상황에 맞춰 고르면 돼요.

### 🪜 진입 방법 비교

| 명령 | 시작 위치 | 언제 쓰면 좋을까 |
|------|----------|-----------------|
| `claude remote-control` | 셸 (서버 모드) | 데스크탑을 켜두고 **나갔다 올 때** |
| `claude --remote-control "이름"` | 셸 (인터랙티브) | 평소처럼 작업하면서 **이름 붙여 시작** |
| `/remote-control` | 이미 실행 중인 세션 | 작업하다가 **갑자기 자리 비울 때** |

### 🎬 시나리오별 명령

#### 🍎 macOS / 🐧 Linux

```bash
# 패턴 A: 그냥 서버만 띄우기
claude remote-control

# 패턴 B: 이름 붙여 인터랙티브 시작
claude --remote-control "side-project"

# 패턴 C: 작업 중에 켜기 (Claude 안에서)
/remote-control
```

#### 🪟 Windows (PowerShell)

```powershell
# PowerShell 에서도 동일한 명령
claude remote-control
claude --remote-control "side-project"
```

> 💡 셸이 다르더라도 명령은 동일해요. PowerShell 특유의 인용 처리만 주의하면 OK.

### 📱 모바일에서 연결 (QR 스캔)

진입 명령을 실행하면 터미널에 **세션 URL** 과 **QR 코드** 가 표시돼요.

```
Connected.
Session URL : https://claude.ai/code/sessions/xxxxxxxx
[QR 코드 박스]

Press <SPACE> to toggle QR.
```

1. 폰의 Claude 앱 열기
2. 좌상단 메뉴 → **"Scan to connect"** 또는 카메라 아이콘
3. 터미널의 QR 코드 스캔
4. 앱에 세션이 뜨면 메시지 입력 시작 ✨

> 💡 QR 이 너무 크면 스페이스바로 토글해서 끄고 URL 만 복사해도 돼요.

### 🔁 매 세션 자동 활성화

매번 `/remote-control` 치기 귀찮다면 한 번만 설정해 두세요.

```
/config
```

→ **"Enable Remote Control for all sessions"** 항목을 `true` 로 토글.

---

## 4️⃣ 어디서 어떻게 제어할까

연결할 수 있는 표면(surface)은 네 가지. 표면마다 **할 수 있는 것** 이 조금씩 달라요.

### 🪟 표면별 능력 비교

| 표면 | 메시지 보내기 | 도구 승인 | 파일 자동완성 (`@`) | 푸시 알림 |
|------|:---:|:---:|:---:|:---:|
| 🌐 **claude.ai/code** (웹) | ✅ | ✅ | ✅ | — |
| 📱 **iOS** Claude 앱 | ✅ | ✅ | 부분 | ✅ |
| 🤖 **Android** Claude 앱 | ✅ | ✅ | 부분 | ✅ |
| 🧩 **VS Code** 확장 | ✅ | ✅ | ✅ | — |
| 💻 **로컬 터미널** | ✅ | ✅ | ✅ | OS 알림 |

### 🎛 슬래시 명령 가능 여부

| 명령 | 원격에서 됨? | 비고 |
|------|:---:|------|
| `/clear`, `/compact`, `/context`, `/usage` | ✅ | 텍스트 출력 위주 |
| `/exit`, `/rename` | ✅ | 세션 메타 조작 |
| `/mcp` | ❌ | **로컬 전용** — 키 입력 인터랙션 필요 |
| `/plugin` | ❌ | **로컬 전용** |
| `/resume` | ❌ | **로컬 전용** |

> 💡 **외울 필요 없는 규칙**: **"화살표 키로 골라야 하는 명령은 로컬 전용"** 이라고 기억하면 거의 맞아요.

### 🟢 연결 상태 확인

claude.ai/code 의 세션 목록에서:

- 🟢 **녹색 점**: 로컬 프로세스 살아있고 폴링 중
- ⚪ **회색 점**: 연결 끊김 (노트북 잠금·네트워크 끊김 등)
- 🔴 **빨간 점**: 만료 또는 종료됨

---

## 5️⃣ 실전 시나리오: 카페에서 빌드 확인하기

🎬 한 번의 흐름으로 익혀봐요.

### 상황

- 점심시간 직전, 회사 데스크탑에서 큰 리팩토링을 진행 중
- 빌드와 테스트가 5~10분 걸림
- 점심 먹으면서 폰으로 결과만 확인하고 승인하고 싶어요

### 흐름

#### Step 1. 데스크탑에서 켜두고 나가기

작업 세션 안에서:

```
/remote-control
```

→ 터미널에 URL/QR 표시. 노트북 잠그지 말고 **자리만 비우기**.

> 💡 Mac 의 경우 시스템 환경설정에서 "디스플레이 끔 ≠ 잠금" 으로 설정해두면 안전해요.

#### Step 2. 폰에서 접속

- Claude 앱 → 세션 목록 → 🟢 점이 떠 있는 내 세션 탭
- 또는 QR 을 미리 스캔해 둔 상태라면 곧장 들어가요

#### Step 3. 카페에서 메시지 전송

```
빌드 끝났어? 테스트 통과했으면 lint 도 돌려줘.
```

→ 데스크탑이 명령을 받아 빌드/테스트/lint 실행.

#### Step 4. 푸시 알림으로 결정 요청 받기

테스트 중 Claude 가 위험해 보이는 파일 삭제를 시도 → 폰에 푸시:

```
🔔 Claude is requesting permission:
   Delete src/legacy/old-auth.ts
```

→ 폰에서 **승인 / 거부** 한 번 탭 → 데스크탑 작업 재개 ✨

#### Step 5. 사무실 복귀 후 그대로 이어쓰기

데스크탑 터미널로 돌아오면 같은 세션에 폰에서 보낸 대화가 그대로 보여요. 그냥 이어서 입력하면 끝.

> 💎 **이 시나리오의 핵심**: 코드는 데스크탑에서만 실행됐고, 파일은 한 번도 클라우드에 올라가지 않았어요. 폰은 **창** 일 뿐.

---

## 6️⃣ 보안과 한계

### 🔐 보안 모델 한 장 요약

```
┌────────────────┐    outbound HTTPS only    ┌────────────────┐
│  내 컴퓨터      │ ────────────────────────▶ │ Anthropic API   │
│ (Claude Code)  │ ◀──── poll for work ──── │   (게이트웨이)   │
└────────────────┘                           └────────────────┘
                                                     ▲
                                                     │ HTTPS
                                                     │
                                              ┌──────────────┐
                                              │ 폰 / 브라우저 │
                                              └──────────────┘
```

| 요소 | 설명 |
|------|------|
| 🚫 **인바운드 포트 X** | 내 컴퓨터는 **밖으로만** 연결을 만들어요. 라우터·방화벽 설정 필요 없음. |
| 🔒 **TLS 전구간 암호화** | 일반 Claude Code 트래픽과 동일 (Anthropic API 경유). |
| ⏱ **단명 자격증명** | 연결마다 짧은 수명 토큰, 용도별로 분리 발급, 독립 만료. |
| 🖥 **코드 실행은 로컬** | 클라우드로 코드가 빠져나가지 않아요. 파일 시스템·MCP 도 그대로. |
| 🪟 **창과 본체 분리** | 폰/브라우저는 메시지/결과를 보여주는 "창", 본체는 내 컴퓨터. |

### ⚠️ 현재 한계

| 한계 | 의미 |
|------|------|
| ⏱ **네트워크 끊김 ~10분** | 그 이상 끊기면 세션이 만료돼요. |
| 🛏 **로컬 프로세스 필요** | 노트북을 잠그거나 끄면 즉시 회색 점. |
| ☁️ **Bedrock / Vertex / Foundry 와 함께 X** | 클라우드 모델 라우팅 사용 중이면 동작 안 해요. |
| 🧠 **Ultraplan 시작 시 연결 해제** | 장기 자동 실행 모드 진입하면 원격 끊김. |
| 🎛 **인터랙티브 슬래시 명령 일부 X** | `/mcp`, `/plugin`, `/resume` 는 로컬에서. |
| 🔄 **세션 1개 (인터랙티브)** | 서버 모드는 `--capacity` 로 다중 가능, 인터랙티브는 1개. |

> 💡 한계를 이해하고 쓰면 답답하지 않아요. **"내 컴퓨터가 켜져 있어야 한다"** 는 한 줄만 기억하면 대부분 해결.

---

## 🪞 5분 회고

오늘 배운 걸 머리에 박아두는 시간이에요.

- 🤔 Remote Control 과 SSH 의 가장 큰 차이를 한 문장으로 설명할 수 있나요?
- 🤔 진입 명령 3가지 중 **내 평소 패턴** 에 가장 잘 맞는 건 어떤 것인가요?
- 🤔 폰에서 `/mcp` 가 안 되는 이유를 친구에게 설명한다면?
- 🤔 회사 네트워크 보안팀이 "이거 안전해?" 라고 물으면 어떻게 답할 건가요?

> 💭 **토론거리**
> "Remote Control 이 있다면, 굳이 Codespaces 같은 클라우드 IDE 가 필요할까요?"
> 환경 동기화 / 머신 사양 / 협업 / 보안 관점에서 각자 입장을 정리해보세요.

---

## 🆘 자주 막히는 지점

### Q1. `/remote-control` 이 메뉴에 안 떠요

**원인 1**: 버전이 낮음.

```bash
claude --version    # v2.1.51 이상이어야 함
claude update
```

**원인 2**: API 키로 로그인됨. claude.ai OAuth 로 다시 로그인.

```
/login    →  claude.ai 선택
```

**원인 3**: Team / Enterprise 인데 관리자 토글이 꺼져 있음. 관리자에게 `claude.ai/admin-settings/claude-code` 의 Remote Control 토글을 켜달라고 요청.

### Q2. QR 코드를 폰으로 스캔했는데 "세션을 찾을 수 없음"

- 데스크탑 쪽 명령(`claude remote-control` 등)이 **여전히 실행 중** 인지 확인하세요. 닫혔다면 세션도 사라져요.
- VPN/회사 프록시가 Anthropic API 도메인을 막고 있는 경우도 있어요. `code.claude.com`, `api.anthropic.com` 으로의 outbound HTTPS 가 열려야 해요.

### Q3. 노트북 덮개를 닫으니 회색 점이 떴어요

OS 의 **수면(sleep) 모드** 가 들어가면 프로세스가 멈춰요.

- 🍎 macOS: `caffeinate -i claude remote-control` 로 실행하면 슬립 방지
- 🐧 Linux: `systemd-inhibit --what=idle:sleep claude remote-control`
- 🪟 Windows: 전원 옵션 → 덮개 닫을 때 동작 → "아무 작업도 안 함" 으로 변경

10분 이내 깨어나면 자동 재연결되지만, 그 이상이면 세션을 다시 시작해야 해요.

### Q4. 폰에서 푸시가 안 와요

```
/config
```

→ **"Push when Claude decides"** 항목이 ON 인지 확인. 그리고 폰 OS 의 알림 권한도 Claude 앱에 허용돼 있어야 해요.

### Q5. 회사 정책상 인바운드 포트 못 여는데 동작할까요?

✅ **됩니다.** Remote Control 은 **outbound HTTPS 만** 사용해요. 일반적인 사내망에서 이미 허용된 트래픽이라 추가 방화벽 변경 없이 동작하는 게 보통이에요.

---

## 🎯 오늘 배운 핵심 정리

| 항목 | 핵심 |
|------|------|
| 🪟 정체 | 로컬 Claude Code 세션의 **원격 창** |
| 💼 플랜 | Pro / Max / Team / Ent (Free X) |
| 🔑 인증 | claude.ai OAuth **필수**, API key 미지원 |
| 🚪 진입 | `claude remote-control` / `--remote-control` / `/remote-control` |
| 📱 표면 | 웹·iOS·Android·VS Code·로컬 |
| 🚫 로컬 전용 | `/mcp`, `/plugin`, `/resume` |
| 🔐 보안 | outbound HTTPS only, 코드는 로컬, 단명 토큰 |
| ⏱ 한계 | 네트워크 끊김 ~10분, 로컬 프로세스 필수, Bedrock/Vertex 와 X |
| 🧪 상태 | Research preview |

> 💎 한 문장으로: **"Remote Control 은 내 컴퓨터를 클라우드로 옮기는 게 아니라, 내 컴퓨터에 다른 화면 하나를 더 붙이는 일이에요."**

---

## 📚 공식 문서

- [Remote Control 개요](https://code.claude.com/docs/en/remote-control)
- [CLI 레퍼런스](https://code.claude.com/docs/en/cli-reference)
- [보안 모델](https://code.claude.com/docs/en/security)
- [인증](https://code.claude.com/docs/en/authentication)

---

## 🎓 다음 단계

- 📘 [Skills 시스템 & Remote Control 결합 가이드](./skills-and-remote-control.md) — Skills 와 Remote Control 을 함께 다루는 더 넓은 맥락
- 📘 [효과적인 프롬프트 & CLAUDE.md 실습](./effective-prompting-and-claude-md.md) — 원격에서 짧게 던지는 프롬프트일수록 CLAUDE.md 가 중요해요
- 📘 [슬래시 명령 레퍼런스](./1-3.claude_commands-reference.md) — 어떤 명령이 원격에서 동작하는지 종합 정리
- 📘 [상태 읽기 가이드](./status-reading-guide.md) — 폰 작은 화면에서도 `/status` 출력을 빠르게 읽는 팁

---

## 🌱 마무리: Fail Forward

오늘 어디까지 갔는지 색깔로 표시해보세요.

- 🟢 **잘된 부분** — `/remote-control` 켜고 폰에서 메시지 전송까지 성공
- 🟡 **애매한 부분** — QR 스캔은 됐는데 푸시 알림이 안 오거나, 자리 비웠더니 슬립 들어감
- 🔴 **막힌 부분** — 관리자 토글 / 회사 VPN / API 키 로그인 같은 환경 이슈

```markdown
## 🌱 Remote Control 학습 노트 — 2026.MM.DD

### 🟢 잘된 부분
- (예시) `claude --remote-control "side"` 로 이름 붙여 시작 → 폰에서 바로 보임

### 🟡 애매한 부분
- (예시) 푸시는 오는데 어떤 도구 호출에서 오는지 헷갈림 → /config 다시 보기

### 🔴 막힌 부분
- (예시) 노트북 잠그면 끊겨서, 회의 끝나고 와도 세션 이어가기 실패
  - 다음 시도: caffeinate / 전원 옵션 변경
```

> 💎 **막힘은 실패가 아니라 다음 실험의 입력값이에요.** 막혔으면 그 자리만 빨갛게 칠해두고 내일 한 줄만 더 시도해보세요.

---

📅 작성일: 2026.05.05
✍️ Claude Code 강의 실습용 환경 구축 가이드 시리즈
